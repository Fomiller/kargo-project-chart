project: { name: bad, repoURL: https://github.com/Fomiller/homelab.git }
services:
  - name: svc
    image: { repoURL: example.com/svc }
warehouses:
  - name: app
    services: all
stages:
  - name: dev
    channel: rc
    warehouses: [app]
    git: { branch: main }
