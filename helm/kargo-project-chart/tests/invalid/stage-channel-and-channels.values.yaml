project: { name: bad, repoURL: https://github.com/Fomiller/homelab.git }
services:
  - name: svc
    image: { repoURL: example.com/svc }
warehouses:
  - name: app
    services: all
    channels: [release, rc]
stages:
  - name: dev
    channel: rc
    channels: [release, rc]
    warehouses: [app]
    env: dev
    git: { branch: main }
